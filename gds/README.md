# gds/

Empty for now. Once layout + DRC/LVS/PEX are clean, this folder gets:

- `tt_um_catalinlazar_ringamp_bgap_ihp.gds`
- `tt_um_catalinlazar_ringamp_bgap_ihp.lef`

Filenames must exactly match `top_module` in `info.yaml` and the repo name.
